// Assuming you have a function to get the drink count
function getDrinkCount() {
    // This function should return the number of drinks the current user has ordered.
    // For now, we'll just return a placeholder value.
    return 0;
  }
  
  // Update the drink count on page load
  document.getElementById('drink-count-number').textContent = getDrinkCount();
  